<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="VisioAnalytica.App.Risk.MainPage"
             Title="Inicio">

    <ScrollView>
        <VerticalStackLayout
            Padding="20,30"
            Spacing="24">
            
            <Label
                Text="VisioAnalytica Risk"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1"
                HorizontalOptions="Center"
                Margin="0,0,0,8" />

            <Label
                Text="Análisis de Riesgos con IA"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                HorizontalOptions="Center"
                TextColor="{StaticResource Gray600}"
                Margin="0,0,0,24" />
            
            <!-- Card de Nueva Inspección -->
            <Border Stroke="{StaticResource Primary}"
                    StrokeThickness="2"
                    StrokeShape="RoundRectangle 16"
                    Padding="24"
                    BackgroundColor="{StaticResource Gray50}"
                    Margin="0,0,0,16">
                <VerticalStackLayout Spacing="16">
                    <HorizontalStackLayout>
                        <Label Text="📸"
                               FontSize="32"
                               VerticalOptions="Center" />
                        <Label Text="Nueva Inspección"
                               FontSize="20"
                               FontAttributes="Bold"
                               TextColor="{StaticResource Gray900}"
                               VerticalOptions="Center"
                               Margin="12,0,0,0" />
                    </HorizontalStackLayout>
                    
                    <Label Text="Captura múltiples fotos y analízalas con IA para identificar riesgos laborales"
                           FontSize="14"
                           TextColor="{StaticResource Gray600}"
                           Margin="0,0,0,12" />
                    
                    <Button x:Name="CaptureButton"
                            Text="Iniciar Inspección"
                            Clicked="OnCaptureClicked"
                            BackgroundColor="{StaticResource Primary}"
                            TextColor="White"
                            CornerRadius="10"
                            HeightRequest="56"
                            FontSize="18"
                            FontAttributes="Bold" />
                </VerticalStackLayout>
            </Border>
            
            <!-- Card de Historial -->
            <Border Stroke="{StaticResource Gray300}"
                    StrokeThickness="1"
                    StrokeShape="RoundRectangle 16"
                    Padding="24"
                    BackgroundColor="White"
                    Margin="0,0,0,16">
                <VerticalStackLayout Spacing="16">
                    <HorizontalStackLayout>
                        <Label Text="📋"
                               FontSize="32"
                               VerticalOptions="Center" />
                        <Label Text="Historial de Inspecciones"
                               FontSize="20"
                               FontAttributes="Bold"
                               TextColor="{StaticResource Gray900}"
                               VerticalOptions="Center"
                               Margin="12,0,0,0" />
                    </HorizontalStackLayout>
                    
                    <Label Text="Revisa inspecciones anteriores, sus resultados y el estado de los análisis"
                           FontSize="14"
                           TextColor="{StaticResource Gray600}"
                           Margin="0,0,0,12" />
                    
                    <Button x:Name="HistoryButton"
                            Text="Ver Historial"
                            Clicked="OnHistoryClicked"
                            BackgroundColor="Transparent"
                            TextColor="{StaticResource Primary}"
                            BorderColor="{StaticResource Primary}"
                            BorderWidth="2"
                            CornerRadius="10"
                            HeightRequest="56"
                            FontSize="18" />
                </VerticalStackLayout>
            </Border>

            <Button
                x:Name="LogoutButton"
                Text="Cerrar Sesión"
                Clicked="OnLogoutClicked"
                BackgroundColor="Transparent"
                TextColor="Red"
                HeightRequest="44"
                Margin="0,8,0,0" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
